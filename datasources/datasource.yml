apiVersion: 1

datasources:
  - name: influxDB
    type: influxdb
    access: server
    url: http://192.168.230.128:8086
    basicAuth: true
    basicAuthUser: admin
    basicAuthPassword: admin123456
    organization: ${INFLUXDB_ORG}
    defaultBucket: ${INFLUXDB_BUCKET}
editable: true